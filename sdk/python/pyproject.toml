[tool.poetry]
name = "motionmesh"
version = "0.1.0"
description = "Python SDK for MotionMesh"
authors = ["MotionMesh Team"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
